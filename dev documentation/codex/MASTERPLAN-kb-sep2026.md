# MASTERPLAN M: Обновление KB по сводке законодательства (сент. 2026)

**Status:** ✅ done — KB + Vector Store 29/29 completed (2026-09-01)  
**Дата:** 2026-09-01  
**Critic:** [KB critic](56cf5da0-f2c7-43a4-b89f-a9f1ad1b502c)  
**Повод:** сводка изменений; Олег дал файлы НПА + вариант **A** (полная замена в KB + Vector Store).

**Hard gate 182 (зафиксировано):** 182 отменяет 1043 + 624 + 241 → `1043` и `241` в `future_extension/`.  
**227:** отменяет 1028 + 825 → `1028` в `future_extension/`.

## Входные файлы (Downloads)

| Файл | Роль в KB |
|------|-----------|
| `Приказ … N 242 … ФККО.rtf` | **Полная замена** `kb/242-PR-RPN-…fkko.md` |
| `Приказ … N 227 … учёт отходов.rtf` | **Новый** акт учёта отходов; **заменяет** `kb/1028-…` |
| `Приказ … N 182 … декларация о плате.rtf` | **Новый** порядок/форма декларации о плате за НВОС; **заменяет/снимает** актуальные `1043` + связанные `241` (уточнить по тексту 182) |
| `Письмо … от 30 июня 2026.odt` | **Не отдельный NPA в VS** — разъяснение РПН по НДВ → FAQ Q&A + при необходимости Warning в `581` |

## Решения продукта (от Олега)

1. Вариант **A** — полноценные НПА в `kb/` + `--force` upload.  
2. НДВ — разъяснение (письмо), не новый приказ.  
3. ФККО — **полная перезаливка** каталога из нового дампа 242.

## Патчи критика (обязательны)

1. **Запрещён полный `process_kb.py` по всему `raw/`.** Только изолированный `raw_batch_sep2026/` + одноразовый прогон с подменой RAW_DIR **или** ручная сборка md по YAML-шаблону.
2. Новый raw 242 **заменяет** старый raw 242 (не два файла №242). Superseded убрать из активного `raw/`.
3. **Hard gate 182:** до upload зафиксировать: 182 заменяет пакет 1043+241 → оба в `future_extension/` **или** иное — строкой в отчёте. Без решения — стоп M3.
4. Почистить stale «1028/1043 как действующие» в FAQ (+ минимум `services.md` / `NVOS-Ref` при явных утверждениях).
5. Письмо РПН — **только FAQ** (+ Warning в `581`); **запрет** `letter*` как `kb/*.md`.
6. `TODAY`: точечно `updated: 2026-09-01` на новых/заменённых; не full-run с новым TODAY.
7. DoD: один `242-*.md`; нет 1028 в `kb/*.md`; letter md отсутствует; VS все completed вкл. бывший fail `416-FZ`; smoke не цитирует 1028/1043 как актуальные; даты силы — из текста приказов.
8. FKKO: контроль UTF-8/размера; при upload fail — retry до полного completed.

## Шаги

### M1 — Приёмка файлов в `raw/`

1. Скопировать 4 файла в `raw/` с ASCII/латинскими именами (кириллица в CLI Windows ломается), например:
   - `npa__rpn__PR_RPN_22_05_2017_242_fkko.rtf` (или сразу `.txt` после конвертации)
   - `npa__mpr__PR_MPR_16_04_2026_227_uchet_othodami.rtf`
   - `npa__mpr__PR_MPR_01_04_2026_182_deklaraciya_plata.rtf`
   - `letter__rpn_30_06_2026_ndv.odt` → txt
2. Конвертировать RTF/ODT → UTF-8 `.txt` (Word/LibreOffice/pandoc/striprtf — что доступно).  
3. Не коммитить бинарники RTF в git, если огромные — в git только итоговые `kb/*.md` (или raw txt по принятой практике репо).

### M2 — Обработка в `kb/`

1. Прогнать `process_kb.py` **только на новые txt** *или* точечно сконвертировать в md по тому же YAML-шаблону, что у существующих файлов (если полный прогон по всему raw опасен/долгий). Предпочтительно: добавить txt в raw и обработать целевые документы без порчи остальных `kb/*.md`.  
2. Обновить `TODAY` в `process_kb.py` на **2026-09-01** для frontmatter `updated` у новых файлов (или вручную выставить в YAML).  
3. Имена:
   - `227-PR-Minprirody-…ucheta…othodami.md`
   - `182-PR-Minprirody-…deklaracii-plate-za….md`
   - заменить содержимое `242-PR-RPN-…fkko.md` (тот же stem, новый текст)
4. Старые:
   - `1028-…md` → переместить в `future_extension/` (superseded с 01.09.2026)
   - `1043-…` и `241-…`: если 182 **признаёт утратившими силу** / заменяет порядок и форму — тоже в `future_extension/`; если 182 только меняет форму, а 1043 остаётся базой — обновить Warnings и оставить (решение по тексту приказа 182 на шаге M2).
5. FAQ: добавить Q&A:
   - учёт отходов: с 01.09.2026 действует пр. №227 вместо №1028;
   - декларация о плате: новый порядок пр. №182 с 01.09.2026 (формулировки строго по тексту);
   - ФККО: каталог обновлён (выписка/редакция из дампа 242; не выдумывать даты изменений без текста);
   - НДВ: краткое содержание письма РПН от 30.06.2026 (разъяснение, не новый НПА).
6. Warnings в затронутых файлах: «не путать №1028 (архив) с №227».

### M3 — Vector Store

1. VPN on.  
2. `py scripts/upload_kb_to_openai_vector_store.py --force`  
3. Дождаться all `completed` в `vector_store_upload_report.md` (прошлый прогон: `416-FZ` failed — перепроверить).  
4. VPS deploy **не** нужен только для VS; нужен только если меняем prompt/server (не планируем).

### M4 — Проверка

1. Пересчёт: в `kb/*.md` есть 227, 182, новый 242; нет 1028 (и 1043/241 — по решению M2).  
2. Smoke RAG (локально или eval): вопросы про учёт отходов / декларацию платы / ФККО / НДВ — бот ссылается на новые номера.  
3. Report `dev documentation/codex/reports/013-kb-sep2026-update.md` + «что не удалось».

## Caps / запреты

- Не хардкодить API keys; не логировать секреты.  
- Не сокращать юридический текст НПА.  
- Не использовать t.me / dzen как source.  
- Не слать в прод админку без ок.  
- FKKO файл большой (~8MB RTF) — таймауты upload 600s уже в скрипте; при fail — retry `--force` или пофайлово задокументировать.

## Очередь

| Шаг | Кто | Что |
|-----|-----|-----|
| 0 | Cursor | Masterplan + критик |
| 1 | Codex | M1–M2 + FAQ + report 013 (без upload если нет VPN) |
| 2 | Cursor/Codex | M3 `--force` upload при VPN |
| 3 | Cursor | DoD пересчёт + STATUS |

## Success DoD

- [ ] `kb/` содержит актуальные 227, 182, обновлённый 242  
- [ ] 1028 (и при необходимости 1043/241) вне активного корпуса (`future_extension/`)  
- [ ] FAQ отражает 4 темы сводки без TG/Dzen ссылок  
- [ ] Vector Store `--force` зелёный (все completed)  
- [ ] Report 013 + «что не удалось»

## Out of scope

- Виджет / Метрика / Bitrix  
- Скрейп статей сайта  
- Правка `mini_assessment` без отдельного запроса  
