# Codex 004 — mobile close + catalog load

## Files changed

- `widget/src/01-config.js` — added `assetBaseUrl`; bumped `WIDGET_VERSION` to `1.5.45`.
- `widget/src/02-data-layer.js` — added asset-origin fallback and two-attempt retry for JSON and markdown fetches, with `credentials: "omit"`.
- `widget/src/11-static-dom.js` — moved popup close control into the header.
- `widget/src/15-screens.js` — added `Повторить` and `Закрыть` to catalog error and a `Закрыть` escape hatch to intro.
- `styles.css` — sticky opaque header, high-contrast 44×44 mobile close control, fullscreen mobile overlay and safe-area padding.
- `app.js` — regenerated by the widget build.

## Mobile close behavior

The popup header contains the logo and a visible close button. The header is sticky inside the scrollable popup, so the control remains available while the body scrolls. The mobile overlay is fixed fullscreen above the host page. Desktop backdrop click-to-close remains enabled.

## Catalog asset origin

Asset resolution now uses, in order: the script directory captured at parse time, `ECOLEADBOT_CONFIG.assetBaseUrl`, then the current page location. Andrey should set this in the site config:

```js
assetBaseUrl: "https://elb.ecolusspb.ru/"
```

## Version and build

Version: `1.5.45`.

Build command: `py scripts/build_widget.py`.

Build result: passed; `app.js` regenerated from 27 source fragments.

Syntax check: passed; `node --check app.js`.

## Manual test steps

1. On an iPhone, open the widget from the embedded `ecolusspb.ru` page, scroll the popup, and verify the header close control remains visible and closes the popup.
2. On intro and catalog-error screens, verify the text `Закрыть` controls close the popup.
3. Select `Нужна конкретная услуга / документ`; verify catalog requests target `https://elb.ecolusspb.ru/data/...` when `assetBaseUrl` is configured and recover after a transient failed request.
4. Verify the catalog error screen offers `Повторить`, the main flow, RAG, and `Закрыть`.
