# Логи локального dev-сервера
serve.out.log
serve.err.log
*.log

# Python
__pycache__/
*.pyc
*.pyo

# Настройки редактора
.vscode/

# Системные файлы ОС
.DS_Store
Thumbs.db
desktop.ini

# Временные файлы
*.tmp

# Секреты
.env

# Локальный конфиг деплоя (SSH-хост, не пароли)
deploy/deploy.config.env

# Архивы деплоя (собираются скриптами, не source of truth)
deploy/**/*.tar.gz
deploy/**/*.zip
deploy/sweb/ecoleadbot-sweb.zip
deploy/sweb/dist/

# Серверный elb-config с секретом (генерируется deploy.ps1 в корне VPS, не коммитить)
/elb-config.js
/elb-config.local.js
/elb-config.secret.js

# Промежуточные данные парсинга (итог — kb/services.md)
reports/services_raw.json

# Локальные CRM/аналитика — не коммитить
*.xls
*.xlsx
bitrix_deals.*
friend_companies.*
intersections_*

# Runtime state отчётов Метрики (живёт на VPS)
data/metrika_state/
data/metrika_report_state.json
